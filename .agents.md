# Codex Pro Max HITL Alias

This project uses `AGENTS.md` as the active Codex instruction file.

Use HITL only when the manager at `C:\Users\ramly\Desktop\CodexProMax` is active. When active, use the helper scripts in `C:\Users\ramly\.codex\skills\codex-pro-max-hitl\scripts`.

Write only a normal Codex conclusion through `request_review.ps1 -Output`, wait, then consume the next instruction with `consume_instruction.ps1`. Continue until `shouldFinish=true`.

Use `session.md` for session history.
